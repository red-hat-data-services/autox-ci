# AutoX Benchmarks - All Dependencies
# Install all: pip install -r requirements.txt
# Benchmarking only: pip install kfp boto3 PyYAML pandas lxml kubernetes huggingface-hub

# ===== Core Benchmarking Dependencies =====
# Required for both AutoML and AutoRAG orchestrators

# Kubeflow Pipelines client
kfp>=2.8.0,<3

# S3 storage interaction
boto3>=1.34.0,<2

# Configuration and data processing
PyYAML>=6.0
pandas>=2.0
lxml>=5.0,<6

# Kubernetes API (for loading ConfigMaps)
kubernetes>=28.0.0

# HuggingFace Hub (for OpenRAGBench dataset downloads)
huggingface-hub>=0.20.0

# ===== Dataset Generation (AutoRAG only) =====
# Required only for scripts/generate_rag_datasets.py
# To install only core dependencies, omit these:

# BEIR benchmark datasets
beir>=2.0.0

# HuggingFace datasets library (for SlideVQA)
datasets>=2.0.0

# Image processing (for SlideVQA slide images)
Pillow>=10.0.0

# HTTP requests (better SSL handling for PDF downloads)
requests>=2.31.0
